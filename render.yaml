# render.yaml – Infrastruktur als Code (Webservice + Postgres)

databases:
  - name: shop-db
    plan: free
    databaseName: shop_db
    user: shop_user

services:
  - type: web
    name: django-shop
    plan: free
    runtime: python
    region: frankfurt
    buildCommand: "./build.sh"
    startCommand: "python -m gunicorn shop.asgi:application -k uvicorn.workers.UvicornWorker --timeout 120 --log-file -"
    autoDeploy: true
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: shop-db
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: "False"
      - key: PYTHON_VERSION
        value: "3.11"
